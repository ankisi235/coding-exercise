package nz.co.tmsandbox.webinteractivities;

public class MasterPage {
    public enum findingBy {
        css,
        xpath,
        id,
        class_name,
        name,
        link_text,
        partial_link_text,
        tag_name
    }
}
